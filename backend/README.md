# Backend — Rails 8 API

See the [root README](../README.md) for setup, and [docs/DECISIONS.md](../docs/DECISIONS.md) for design decisions.

```bash
bin/rails db:prepare     # create + migrate
bundle exec rspec        # specs + coverage gate (>= 90% lines)
bundle exec rubocop      # lint (0 offenses required)
```
